import 'package:flutter/material.dart';

/// S52: 身体情報 - TODO: 実装予定
class BodyInfoScreen extends StatelessWidget {
  const BodyInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('S52: 身体情報')),
      body: const Center(
        child: Text('S52: 身体情報\n（実装予定）', textAlign: TextAlign.center),
      ),
    );
  }
}
