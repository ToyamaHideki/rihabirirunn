import 'package:flutter/material.dart';

/// S51: モード設定 - TODO: 実装予定
class ModeSettingsScreen extends StatelessWidget {
  const ModeSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('S51: モード設定')),
      body: const Center(
        child: Text('S51: モード設定\n（実装予定）', textAlign: TextAlign.center),
      ),
    );
  }
}
