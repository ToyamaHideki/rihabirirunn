import 'package:flutter/material.dart';

/// S30: 走行中ナビ - TODO: 実装予定
class ActiveRunScreen extends StatelessWidget {
  const ActiveRunScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('S30: 走行中ナビ')),
      body: const Center(
        child: Text('S30: 走行中ナビ\n（実装予定）', textAlign: TextAlign.center),
      ),
    );
  }
}
