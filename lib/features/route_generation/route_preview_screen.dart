import 'package:flutter/material.dart';

/// S21: ルートプレビュー - TODO: 実装予定
class RoutePreviewScreen extends StatelessWidget {
  const RoutePreviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('S21: ルートプレビュー')),
      body: const Center(
        child: Text('S21: ルートプレビュー\n（実装予定）', textAlign: TextAlign.center),
      ),
    );
  }
}
