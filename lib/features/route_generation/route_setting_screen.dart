import 'package:flutter/material.dart';

/// S20: ルート設定 - TODO: 実装予定
class RouteSettingScreen extends StatelessWidget {
  const RouteSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('S20: ルート設定')),
      body: const Center(
        child: Text('S20: ルート設定\n（実装予定）', textAlign: TextAlign.center),
      ),
    );
  }
}
